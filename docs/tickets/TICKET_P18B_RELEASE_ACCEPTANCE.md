# P18B 1.0 发布验收

状态：Queued；依赖 P18A。

服务主命题：全环节（最终交付）。

## 范围

- 从干净 tag 构建签名 Windows 10/11 x64 候选，验证源代码、lockfile、安装包、更新清单、签名和 hash 一致。
- 在两台干净 VM 分别执行新用户与现有数据库路径：安装 → 首次说明 → Today → Practice/Inbox → Map → Profile/Goals/Trust → 备份 → 手动更新 → 恢复 → 卸载。
- 验证离线/Tier 0-only、错误 Key、更新源不可用、数据库锁/损坏/版本过新和更新回滚。
- 生成中文/英文发布说明、数据位置、隐私清单、已知 shadow/unfit 门、回滚和支持文档。
- 所有门满足后发布 GitHub Releases 1.0；不自动 push，发布动作仍需用户明确确认。

## 禁区

- 不把 P18A 未通过的科学对象宣传为有效；不隐藏已知限制。
- 不替换已发布同版本二进制，不发布无签名更新。

## 验收

- 所有正式票已完成且 QUEUE 无 In Progress；SPEC §6、前端、桌面、安装、更新、恢复和安全基线全绿。
- 两台 VM 的完整 transcript、截图、产物 hash、签名验证与回滚记录写入票尾。
- `git diff --check` 通过，工作区只含明确排除的用户改动。

## 回滚

发布前删除 draft；发布后若发现阻断问题，撤下更新推荐并发布更高补丁版本，保留 1.0 审计与 hash。
